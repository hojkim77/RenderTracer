# @react-visual-rendering-tracer/shared-types

프론트엔드와 백엔드 간 공유 TypeScript 타입 정의 패키지

## 사용법

```typescript
import { GraphData, ComponentNode, AnalyzeRequest } from '@react-visual-rendering-tracer/shared-types';
```

## 타입 구조

- `graph.ts`: Graph JSON 데이터 구조
- `api.ts`: API 요청/응답 타입

